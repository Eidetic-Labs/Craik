version: 2

build:
  os: ubuntu-24.04
  tools:
    nodejs: "24"
  commands:
    - node --version
    - npm --version
    - cd docs && npm ci
    - cd docs && npm run build
    - mkdir -p "$READTHEDOCS_OUTPUT/html"
    - cp -R docs/build/. "$READTHEDOCS_OUTPUT/html/"

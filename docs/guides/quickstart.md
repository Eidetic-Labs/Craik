# Quickstart

Craik is currently in pre-0.1.0 implementation. The first available command verifies that the package and CLI entry point are installed:

```sh
craik --version
```

Future quickstart steps will cover project registration, case file assembly, policy envelopes, receipts, Stigmem-backed memory, and durable handoffs.

